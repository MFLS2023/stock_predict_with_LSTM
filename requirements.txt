# ============================================
# Stock Prediction with LSTM - Dependencies
# ============================================
# Python Version: 3.11+ (推荐使用 3.11 以获得最佳 PyTorch/TensorFlow 兼容性)

# 核心数据科学库
numpy>=1.23.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.10.0

# 深度学习框架 - GPU版本安装说明见下方
# PyTorch (推荐，更好的性能和易用性)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# TensorFlow/Keras (可选，如果只用PyTorch可不安装)
tensorflow>=2.10.0
keras>=2.10.0

# 可视化库
matplotlib>=3.5.0
mplfinance>=0.12.9b7
backtesting>=0.3.3

# 强化学习库 (Reinforcement Learning Libraries)
pandas-ta>=0.3.14b
stable-baselines3[extra]>=2.0.0
gymnasium>=0.26.0
tensorboard>=2.10.0

# GUI库
PyQt5>=5.15.0
# 注意：如果使用 PyQt6，需要修改代码中的导入语句

# 训练监控工具（可选）
tensorboardX>=2.6
visdom>=0.2.4

# ============================================
# GPU 版本安装说明
# ============================================
# 对于 NVIDIA GPU 用户，建议使用以下命令安装 GPU 版本：

# PyTorch GPU (CUDA 12.1):
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# PyTorch GPU (CUDA 11.8):
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# TensorFlow GPU:
# pip install tensorflow[and-cuda]
# 或者
# pip install tensorflow==2.15.0
# (TensorFlow 2.10+ 自动包含 GPU 支持)

# ============================================
# 快速安装命令
# ============================================
# 1. 创建新的 conda 环境:
#    conda create -n stock_gpu python=3.11 -y
#    conda activate stock_gpu

# 2. 安装基础依赖:
#    pip install -r requirements.txt

# 3. 安装 PyTorch GPU 版本:
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# 4. (可选) 安装 TensorFlow GPU 版本:
#    pip install tensorflow[and-cuda]

# ============================================
# CPU 版本说明
# ============================================
# 如果只需要 CPU 版本，直接运行:
# pip install -r requirements.txt
# 上面列出的包默认就是 CPU 版本
